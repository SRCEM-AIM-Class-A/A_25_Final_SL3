version: '3.9'

services:
  flask:
    build:
      context: ./flask_app
    ports:
      - "5000:5000"
    volumes:
      - ./flask_app:/app

  django:
    build:
      context: ./django_app
    ports:
      - "8000:8000"
    volumes:
      - ./django_app:/app
